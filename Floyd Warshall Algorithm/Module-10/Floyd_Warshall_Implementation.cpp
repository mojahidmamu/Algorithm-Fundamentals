#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, e;
    cin >> n >> e;
    int adj_matrix[n][n];
    while (e--)
    {
        int a, b, c;
        cin >> a >> b >> c;
        
    }
    
    return 0;
}
