#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, e, Query;
    cin >> n >> e >> Query;
    while (e--)
    {
        int a, b, c;
        cin >> a >> b >> c;
    }

    while (Query--)
    {
        int src, dst;
        cin >> src >> dst;
    }
    
    
    return 0;
}
