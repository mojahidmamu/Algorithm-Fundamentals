#include <bits/stdc++.h>
using namespace std;

int N, M;
vector<string> grid;

int main()
{
    cin >> N >> M;
    grid.resize(N);
    for (int i = 0; i < N; i++)
    {
        cin >> grid[i];
    }
    return 0;
}
