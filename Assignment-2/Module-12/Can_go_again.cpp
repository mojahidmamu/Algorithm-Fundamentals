#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N, E;
    cin >> N >> E;
    while (E--)
    {
        int a, b, c;
        cin >> a >> b >> c;
    }
    int source;
    cin >> source;

    int TestCase;
    cin >> TestCase;
    while (TestCase--)
    {
        int dst;
        cin >> dst;
    }

    return 0;
}
