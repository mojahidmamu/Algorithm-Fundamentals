#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N, E;
    cin >> N >> E;
    while (E--)
    {
        int a, b, c;
        cin >> a >> b >> c;
    }

    int TestCase;
    cin >> TestCase;
    while (TestCase--)
    {
        int src, dst;
        cin >> src >> dst;
    }
    return 0;
}
