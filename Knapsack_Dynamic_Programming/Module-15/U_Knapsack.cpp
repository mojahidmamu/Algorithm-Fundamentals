#include <bits/stdc++.h>
using namespace std;

int N, W;
int weight[25], value[25];
int dp[25][105];

int main()
{
    cin >> N >> W;
    for(int i = 0; i < N; i++)
    {
        cin >> weight[i] >> value[i];
    }

    return 0;
}
