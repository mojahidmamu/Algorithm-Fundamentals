#include <bits/stdc++.h>
using namespace std;

int N, W;
int weight[25], value[25];
int dp[25][105];

int main()
{
    cin >> N >> W;
    while (N--)
    {
        cin >> weight[i] >> value[i];
    }

    return 0;
}
