#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N, W;
        cin >> N >> W;
        int weight_arr[N];
        for (int i = 0; i < N; i++)
        {
            cin >> weight_arr[i];
        }
        
        int value_arr[N];
        for (int i = 0; i < N; i++)
        {
            cin >> value_arr[i];
        }
    }

    return 0;
}
