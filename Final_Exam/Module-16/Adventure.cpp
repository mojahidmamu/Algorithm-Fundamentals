#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int N, W;
        cin >> N >> W;
        int weight_arr[W];
        for (int i = 0; i < W; i++)
        {
            cin >> weight_arr[i];
        }
    }

    return 0;
}
