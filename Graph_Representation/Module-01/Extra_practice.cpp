#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N, E; // N for Node and E for Edge
    cin >> N >> E;
    int adj_mat[N][N];
    for (int i = 0; i < N; i++)
    {
        for (int j = 0; j < N; j++)
        {
            adj_mat[i][j] = 0;
        }
    }

    return 0;
}
