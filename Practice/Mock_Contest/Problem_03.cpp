#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // --- your code here ---
    long long int N, T;
    cin >> N >> T;
    while (T--)
    {
        int L, R;
        cin >> L >> R;
    }

    return 0;
}
